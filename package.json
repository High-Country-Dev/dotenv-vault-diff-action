{
  "name": "dotenv-vault-diff-action",
  "version": "1.0.0",
  "description": "GitHub Action to check and comment on dotenv-vault changes in pull requests",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/main.ts -o dist",
    "test": "jest"
  },
  "keywords": [
    "github",
    "action",
    "dotenv",
    "vault",
    "diff"
  ],
  "author": "Declan Nishiyama",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/github": "^5.1.1"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "@vercel/ncc": "^0.36.1",
    "typescript": "^4.9.5"
  }
}
